\title{Descent and Algebraic Spaces}
\taxon{reference}
\author{The Stacks Project Authors}
\meta{source}{Stacks Project}
\meta{url}{https://stacks.math.columbia.edu/tag/0000}
\meta{slug}{spaces-descent}
\tag{stacks-project}
\tag{algebraic-geometry}

\p{Chapter from the \strong{Stacks Project}, an open-source textbook and reference for algebraic geometry.}

\subtree{
\title{Sections}
\ol{
  \li{Introduction}
  \li{Conventions}
  \li{Descent data for quasi-coherent sheaves}
  \li{Fpqc descent of quasi-coherent sheaves}
  \li{Quasi-coherent modules and affines}
  \li{Descent of finiteness properties of modules}
  \li{Fpqc coverings}
  \li{Descent of finiteness and smoothness properties of morphisms}
  \li{Descending properties of spaces}
  \li{Descending properties of morphisms}
  \li{Descending properties of morphisms in the fpqc topology}
  \li{Descending properties of morphisms in the fppf topology}
  \li{Application of descent of properties of morphisms}
  \li{Properties of morphisms local on the source}
  \li{Properties of morphisms local in the fpqc topology on the source}
  \li{Properties of morphisms local in the fppf topology on the source}
  \li{Properties of morphisms local in the syntomic topology on the source}
  \li{Properties of morphisms local in the smooth topology on the source}
  \li{Properties of morphisms local in the 'etale topology on the source}
  \li{Properties of morphisms smooth local on source-and-target}
  \li{Properties of morphisms 'etale-smooth local on source-and-target}
  \li{Descent data for spaces over spaces}
  \li{Descent data in terms of sheaves}
}
}

\subtree{
\title{Key Definitions}
\ul{
  \li{\code{definition-descent-datum-quasi-coherent} (definition)}
  \li{\code{definition-descent-datum-effective-quasi-coherent} (definition)}
  \li{\code{definition-property-morphisms-local} (definition)}
  \li{\code{definition-property-morphisms-local-source} (definition)}
  \li{\code{definition-local-source-target} (definition)}
  \li{\code{definition-etale-smooth-local-source-target} (definition)}
  \li{\code{definition-descent-datum} (definition)}
  \li{\code{definition-descent-datum-for-family-of-morphisms} (definition)}
  \li{\code{definition-pullback-functor} (definition)}
  \li{\code{definition-pullback-functor-family} (definition)}
}
}