\title{Duality for Schemes}
\taxon{reference}
\author{The Stacks Project Authors}
\meta{source}{Stacks Project}
\meta{url}{https://stacks.math.columbia.edu/tag/0000}
\meta{slug}{duality}
\tag{stacks-project}
\tag{algebraic-geometry}

\p{Chapter from the \strong{Stacks Project}, an open-source textbook and reference for algebraic geometry.}

\subtree{
\title{Sections}
\ol{
  \li{Introduction}
  \li{Dualizing complexes on schemes}
  \li{Right adjoint of pushforward}
  \li{Right adjoint of pushforward and restriction to opens}
  \li{Right adjoint of pushforward and base change, I}
  \li{Right adjoint of pushforward and base change, II}
  \li{Right adjoint of pushforward and trace maps}
  \li{Right adjoint of pushforward and pullback}
  \li{Right adjoint of pushforward for closed immersions}
  \li{Right adjoint of pushforward for closed immersions and base change}
  \li{Right adjoint of pushforward for finite morphisms}
  \li{Right adjoint of pushforward for proper flat morphisms}
  \li{Right adjoint of pushforward for perfect proper morphisms}
  \li{Right adjoint of pushforward for effective Cartier divisors}
  \li{Right adjoint of pushforward in examples}
  \li{Upper shriek functors}
  \li{Properties of upper shriek functors}
  \li{Base change for upper shriek}
  \li{A duality theory}
  \li{Glueing dualizing complexes}
  \li{Dimension functions}
  \li{Dualizing modules}
  \li{Cohen-Macaulay schemes}
  \li{Gorenstein schemes}
  \li{Gorenstein morphisms}
  \li{More on dualizing complexes}
  \li{Duality for proper schemes over fields}
  \li{Relative dualizing complexes}
  \li{The fundamental class of an lci morphism}
  \li{Extension by zero for coherent modules}
  \li{Preliminaries to compactly supported cohomology}
  \li{Compactly supported cohomology for coherent modules}
  \li{Duality for compactly supported cohomology}
  \li{Lichtenbaum's theorem}
}
}

\subtree{
\title{Key Definitions}
\ul{
  \li{\code{definition-dualizing-scheme} (definition)}
  \li{\code{definition-good-dualizing} (definition)}
  \li{\code{definition-gorenstein} (definition)}
  \li{\code{definition-gorenstein-morphism} (definition)}
  \li{\code{definition-relative-dualizing-complex} (definition)}
  \li{\code{theorem-lichtenbaum} (theorem)}
  \li{\code{lemma-equivalent-definitions} (lemma)}
  \li{\code{lemma-affine-duality} (lemma)}
  \li{\code{lemma-internal-hom-evaluate-isom} (lemma)}
  \li{\code{lemma-dualizing-schemes} (lemma)}
}
}