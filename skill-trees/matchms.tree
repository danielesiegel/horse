\title{Matchms}

\taxon{skill}

\meta{description}{"Mass spectrometry analysis. Process mzML/MGF/MSP, spectral similarity (cosine, modified cosine), metadata harmonization, compound ID, for metabolomics and MS data processing."}

\subtree{
\title{Matchms}
\subtree{
\title{Overview}
\p{Matchms is an open-source Python library for mass spectrometry data processing and analysis. Import spectra from various formats, standardize metadata, filter peaks, calculate spectral similarities, and build reproducible analytical workflows.}
}

\subtree{
\title{Core Capabilities}
\subtree{
\title{1. Importing and Exporting Mass Spectrometry Data}
\p{Load spectra from multiple file formats and export processed data:}

\pre{\startverb
from matchms.importing import load_from_mgf, load_from_mzml, load_from_msp, load_from_json
from matchms.exporting import save_as_mgf, save_as_msp, save_as_json

# Import spectra
spectra = list(load_from_mgf("spectra.mgf"))
spectra = list(load_from_mzml("data.mzML"))
spectra = list(load_from_msp("library.msp"))

# Export processed spectra
save_as_mgf(spectra, "output.mgf")
save_as_json(spectra, "output.json")
\stopverb}

\p{\strong{Supported formats:}}

\ul{
\li{mzML and mzXML (raw mass spectrometry formats)}
\li{MGF (Mascot Generic Format)}
\li{MSP (spectral library format)}
\li{JSON (GNPS-compatible)}
\li{metabolomics-USI references}
\li{Pickle (Python serialization)}
}

\p{For detailed importing/exporting documentation, consult \code{\startverb references/importing_exporting.md\stopverb}.}
}

\subtree{
\title{2. Spectrum Filtering and Processing}
\p{Apply comprehensive filters to standardize metadata and refine peak data:}

\pre{\startverb
from matchms.filtering import default_filters, normalize_intensities
from matchms.filtering import select_by_relative_intensity, require_minimum_number_of_peaks

# Apply default metadata harmonization filters
spectrum = default_filters(spectrum)

# Normalize peak intensities
spectrum = normalize_intensities(spectrum)

# Filter peaks by relative intensity
spectrum = select_by_relative_intensity(spectrum, intensity_from=0.01, intensity_to=1.0)

# Require minimum peaks
spectrum = require_minimum_number_of_peaks(spectrum, n_required=5)
\stopverb}

\p{\strong{Filter categories:}}

\ul{
\li{\strong{Metadata processing}: Harmonize compound names, derive chemical structures, standardize adducts, correct charges}
\li{\strong{Peak filtering}: Normalize intensities, select by m/z or intensity, remove precursor peaks}
\li{\strong{Quality control}: Require minimum peaks, validate precursor m/z, ensure metadata completeness}
\li{\strong{Chemical annotation}: Add fingerprints, derive InChI/SMILES, repair structural mismatches}
}

\p{Matchms provides 40+ filters. For the complete filter reference, consult \code{\startverb references/filtering.md\stopverb}.}
}

\subtree{
\title{3. Calculating Spectral Similarities}
\p{Compare spectra using various similarity metrics:}

\pre{\startverb
from matchms import calculate_scores
from matchms.similarity import CosineGreedy, ModifiedCosine, CosineHungarian

# Calculate cosine similarity (fast, greedy algorithm)
scores = calculate_scores(references=library_spectra,
                         queries=query_spectra,
                         similarity_function=CosineGreedy())

# Calculate modified cosine (accounts for precursor m/z differences)
scores = calculate_scores(references=library_spectra,
                         queries=query_spectra,
                         similarity_function=ModifiedCosine(tolerance=0.1))

# Get best matches
best_matches = scores.scores_by_query(query_spectra[0], sort=True)[:10]
\stopverb}

\p{\strong{Available similarity functions:}}

\ul{
\li{\strong{CosineGreedy/CosineHungarian}: Peak-based cosine similarity with different matching algorithms}
\li{\strong{ModifiedCosine}: Cosine similarity accounting for precursor mass differences}
\li{\strong{NeutralLossesCosine}: Similarity based on neutral loss patterns}
\li{\strong{FingerprintSimilarity}: Molecular structure similarity using fingerprints}
\li{\strong{MetadataMatch}: Compare user-defined metadata fields}
\li{\strong{PrecursorMzMatch/ParentMassMatch}: Simple mass-based filtering}
}

\p{For detailed similarity function documentation, consult \code{\startverb references/similarity.md\stopverb}.}
}

\subtree{
\title{4. Building Processing Pipelines}
\p{Create reproducible, multi-step analysis workflows:}

\pre{\startverb
from matchms import SpectrumProcessor
from matchms.filtering import default_filters, normalize_intensities
from matchms.filtering import select_by_relative_intensity, remove_peaks_around_precursor_mz

# Define a processing pipeline
processor = SpectrumProcessor([
    default_filters,
    normalize_intensities,
    lambda s: select_by_relative_intensity(s, intensity_from=0.01),
    lambda s: remove_peaks_around_precursor_mz(s, mz_tolerance=17)
])

# Apply to all spectra
processed_spectra = [processor(s) for s in spectra]
\stopverb}
}

\subtree{
\title{5. Working with Spectrum Objects}
\p{The core \code{\startverb Spectrum\stopverb} class contains mass spectral data:}

\pre{\startverb
from matchms import Spectrum
import numpy as np

# Create a spectrum
mz = np.array([100.0, 150.0, 200.0, 250.0])
intensities = np.array([0.1, 0.5, 0.9, 0.3])
metadata = {"precursor_mz": 250.5, "ionmode": "positive"}

spectrum = Spectrum(mz=mz, intensities=intensities, metadata=metadata)

# Access spectrum properties
print(spectrum.peaks.mz)           # m/z values
print(spectrum.peaks.intensities)  # Intensity values
print(spectrum.get("precursor_mz")) # Metadata field

# Visualize spectra
spectrum.plot()
spectrum.plot_against(reference_spectrum)
\stopverb}
}

\subtree{
\title{6. Metadata Management}
\p{Standardize and harmonize spectrum metadata:}

\pre{\startverb
# Metadata is automatically harmonized
spectrum.set("Precursor_mz", 250.5)  # Gets harmonized to lowercase key
print(spectrum.get("precursor_mz"))   # Returns 250.5

# Derive chemical information
from matchms.filtering import derive_inchi_from_smiles, derive_inchikey_from_inchi
from matchms.filtering import add_fingerprint

spectrum = derive_inchi_from_smiles(spectrum)
spectrum = derive_inchikey_from_inchi(spectrum)
spectrum = add_fingerprint(spectrum, fingerprint_type="morgan", nbits=2048)
\stopverb}
}
}

\subtree{
\title{Common Workflows}
\p{For typical mass spectrometry analysis workflows, including:}

\ul{
\li{Loading and preprocessing spectral libraries}
\li{Matching unknown spectra against reference libraries}
\li{Quality filtering and data cleaning}
\li{Large-scale similarity comparisons}
\li{Network-based spectral clustering}
}

\p{Consult \code{\startverb references/workflows.md\stopverb} for detailed examples.}
}

\subtree{
\title{Installation}
\pre{\startverb
uv pip install matchms
\stopverb}

\p{For molecular structure processing (SMILES, InChI):}

\pre{\startverb
uv pip install matchms[chemistry]
\stopverb}
}

\subtree{
\title{Reference Documentation}
\p{Detailed reference documentation is available in the \code{\startverb references/\stopverb} directory:}

\ul{
\li{\code{\startverb filtering.md\stopverb} - Complete filter function reference with descriptions}
\li{\code{\startverb similarity.md\stopverb} - All similarity metrics and when to use them}
\li{\code{\startverb importing_exporting.md\stopverb} - File format details and I/O operations}
\li{\code{\startverb workflows.md\stopverb} - Common analysis patterns and examples}
}

\p{Load these references as needed for detailed information about specific matchms capabilities.}
}
}