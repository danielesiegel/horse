\title{Hunting Credential Stuffing Attacks}

\taxon{skill}

\tag{hunting}

\tag{credential}

\tag{stuffing}

\tag{attacks}

\meta{version}{"1.0"}

\meta{description}{Detects credential stuffing attacks by analyzing authentication logs for login velocity anomalies, ASN diversity, password spray patterns, and geographic distribution of failed logins. Uses statistical analysis on Splunk or raw log data. Use when investigating account takeover campaigns or building detection rules for auth abuse.}

\subtree{
\title{Hunting Credential Stuffing Attacks}
\subtree{
\title{When to Use}
\ul{
\li{When investigating security incidents that require hunting credential stuffing attacks}
\li{When building detection rules or threat hunting queries for this domain}
\li{When SOC analysts need structured procedures for this analysis type}
\li{When validating security monitoring coverage for related attack techniques}
}
}

\subtree{
\title{Prerequisites}
\ul{
\li{Familiarity with security operations concepts and tools}
\li{Access to a test or lab environment for safe execution}
\li{Python 3.8+ with required dependencies installed}
\li{Appropriate authorization for any testing activities}
}
}

\subtree{
\title{Instructions}
\p{Analyze authentication logs to detect credential stuffing by identifying patterns of distributed login failures, high IP diversity, and suspicious ASN distribution.}

\pre{\startverb
import pandas as pd
from collections import Counter

# Load auth logs
df = pd.read_csv("auth_logs.csv", parse_dates=["timestamp"])

# Credential stuffing indicator: many IPs trying few accounts
ip_per_account = df[df["status"] == "failed"].groupby("username")["source_ip"].nunique()
accounts_under_attack = ip_per_account[ip_per_account > 50]
\stopverb}

\p{Key detection indicators:}

\ol{
\li{High unique source IPs per failed username}
\li{Low success rate across many accounts (< 1\%)}
\li{ASN concentration from cloud/proxy providers}
\li{Geographic impossibility (same account, distant locations)}
\li{User-agent uniformity across distributed IPs}
}
}

\subtree{
\title{Examples}
\pre{\startverb
# Password spray: one password tried across many accounts
spray = df[df["status"] == "failed"].groupby(["source_ip", "password_hash"]).agg(
    accounts=("username", "nunique")).reset_index()
sprays = spray[spray["accounts"] > 10]
\stopverb}
}
}