\title{Bitbucket}

\taxon{skill}

\meta{description}{Interact with Bitbucket repositories and pull requests using the BITBUCKET_TOKEN environment variable. Use when working with code hosted on Bitbucket or managing Bitbucket resources via API.}

\p{You have access to an environment variable, \code{\startverb BITBUCKET_TOKEN\stopverb}, which allows you to interact with the Bitbucket API.}

\p{<IMPORTANT> You can use \code{\startverb curl\stopverb} with the \code{\startverb BITBUCKET_TOKEN\stopverb} to interact with Bitbucket's API. ALWAYS use the Bitbucket API for operations instead of a web browser. ALWAYS use the \code{\startverb create_bitbucket_pr\stopverb} tool to open a pull request </IMPORTANT>}

\pre{\startverb
If you encounter authentication issues when pushing to Bitbucket (such as password prompts or permission errors), the old token may have expired. In such case, update the remote URL to include the current token: `git remote set-url origin https://x-token-auth:${BITBUCKET_TOKEN}@bitbucket.org/username/repo.git`
\stopverb}

\p{Here are some instructions for pushing, but ONLY do this if the user asks you to:}

\ul{
\li{NEVER push directly to the \code{\startverb main\stopverb} or \code{\startverb master\stopverb} branch}
\li{Git config (username and email) is pre-set. Do not modify.}
\li{You may already be on a branch starting with \code{\startverb openhands-workspace\stopverb}. Create a new branch with a better name before pushing.}
\li{Use the \code{\startverb create_bitbucket_pr\stopverb} tool to create a pull request, if you haven't already}
\li{Once you've created your own branch or a pull request, continue to update it. Do NOT create a new one unless you are explicitly asked to. Update the PR title and description as necessary, but don't change the branch name.}
\li{Use the main branch as the base branch, unless the user requests otherwise}
\li{After opening or updating a pull request, send the user a short message with a link to the pull request.}
\li{Do NOT mark a pull request as ready to review unless the user explicitly says so}
\li{Do all of the above in as few steps as possible. E.g. you could push changes with one step by running the following bash commands:}
}

\pre{\startverb
git remote -v && git branch # to find the current org, repo and branch
git checkout -b create-widget && git add . && git commit -m "Create widget" && git push -u origin create-widget
\stopverb}