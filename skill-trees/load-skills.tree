\title{Load Skills}

\taxon{skill}

\meta{version}{1.0.0}

\meta{description}{Bootstrap skill loader for instance startup. Pulls fresh skills from plurigrid/asi and loads GF(3)-balanced startup triads. MUST run at beginning of every new instance.}

\subtree{
\title{Load Skills (Instance Bootstrap)}
\p{\strong{MANDATORY first action on every new Claude Code instance.}}

\subtree{
\title{Startup Protocol}
\pre{\startverb
# Step 1: Pull fresh skills (Rule -1 from AGENTS.md)
npx ai-agent-skills install plurigrid/asi --agent claude

# Step 2: Verify installation
ls ~/.claude/skills | wc -l  # Should be 280+
\stopverb}
}

\subtree{
\title{Critical Startup Triads}
\p{Load these 9 skills in 3 GF(3)-balanced triads:}

\subtree{
\title{Triad 1: Infrastructure (Σ = 0)}
\pre{\startverb
| Skill | Trit | Role | Purpose |
|-------|------|------|---------|
| gay-mcp | +1 | PLUS | Deterministic color/seed generation |
| ruler-maximal | 0 | ERGODIC | Skill loading orchestration |
| skill-validation-gf3 | -1 | MINUS | GF(3) conservation verification |
\stopverb}
}

\subtree{
\title{Triad 2: Dispersal (Σ = 0)}
\pre{\startverb
| Skill | Trit | Role | Purpose |
|-------|------|------|---------|
| parallel-fanout | +1 | PLUS | Maximum parallelization (Rule 1) |
| bisimulation-game | 0 | ERGODIC | Skill dispersal across agents |
| spi-parallel-verify | -1 | MINUS | Parallelism safety verification |
\stopverb}
}

\subtree{
\title{Triad 3: Execution (Σ = 0)}
\pre{\startverb
| Skill | Trit | Role | Purpose |
|-------|------|------|---------|
| aptos-agent | +1 | PLUS | Chain transaction execution |
| mcp-spec-checker | 0 | ERGODIC | Protocol validation |
| say-narration | -1 | MINUS | Voice announcements (constraints) |
\stopverb}
}
}

\subtree{
\title{Voice Announcement (Rule 2)}
\p{After loading each triad, announce via \code{\startverb _\stopverb} (say-narration resolves voice):}

\pre{\startverb
# All announcements use _ - say-narration picks non-English voice
say -v _ "Triad one loaded. Infrastructure ready."
say -v _ "Triad two loaded. Dispersal active."
say -v _ "Triad three loaded. Execution enabled."
\stopverb}

\p{\strong{Note:} This skill DEPENDS on say-narration for voice selection.}
}

\subtree{
\title{Verification}
\pre{\startverb
# Verify GF(3) conservation
# Sum of all 9 skill trits = (+1+0-1) + (+1+0-1) + (+1+0-1) = 0 ✓
echo "GF(3) sum: 0 (conserved)"
\stopverb}
}

\subtree{
\title{Load Order}
\ol{
\li{\code{\startverb gay-mcp\stopverb} - Seeds all color assignments}
\li{\code{\startverb ruler-maximal\stopverb} - Orchestrates subsequent loading}
\li{\code{\startverb skill-validation-gf3\stopverb} - Validates before proceeding}
\li{\code{\startverb parallel-fanout\stopverb} - Enables parallelism}
\li{\code{\startverb bisimulation-game\stopverb} - Disperses to other agents}
\li{\code{\startverb spi-parallel-verify\stopverb} - Verifies parallelism safety}
\li{\code{\startverb aptos-agent\stopverb} - Ready for chain operations}
\li{\code{\startverb mcp-spec-checker\stopverb} - Validates MCP protocols}
\li{\code{\startverb say-narration\stopverb} - Announces completion}
}
}

\subtree{
\title{Integration with ruler-maximal}
\p{This skill bootstraps ruler-maximal, which then handles:}

\ul{
\li{Loading 3 NEW skills per interaction (Rule 0)}
\li{Maximum parallelization (Rule 1)}
\li{Say narration (Rule 2)}
\li{GF(3) color logic (Rule 3)}
}
}

\subtree{
\title{Babashka Bootstrap Script}
\pre{\startverb
#!/usr/bin/env bb
(ns load-skills
  (:require [babashka.process :refer [shell]]))

(def startup-triads
  [{:name "Infrastructure"
    :skills [{:name "gay-mcp" :trit 1}
             {:name "ruler-maximal" :trit 0}
             {:name "skill-validation-gf3" :trit -1}]}
   {:name "Dispersal"
    :skills [{:name "parallel-fanout" :trit 1}
             {:name "bisimulation-game" :trit 0}
             {:name "spi-parallel-verify" :trit -1}]}
   {:name "Execution"
    :skills [{:name "aptos-agent" :trit 1}
             {:name "mcp-spec-checker" :trit 0}
             {:name "say-narration" :trit -1}]}])

(defn verify-gf3 [triad]
  (let [sum (reduce + (map :trit (:skills triad)))]
    (zero? (mod sum 3))))

(defn load-triad! [triad]
  (println (format "Loading %s triad..." (:name triad)))
  (assert (verify-gf3 triad) "GF(3) violation!")
  (doseq [skill (:skills triad)]
    (println (format "  %s (%+d)" (:name skill) (:trit skill))))
  (shell "say" "-v" "_" (format "Triad %s loaded." (:name triad))))

(defn -main []
  ;; Step 1: Fresh pull
  (println "Pulling fresh skills from plurigrid/asi...")
  (shell "npx" "ai-agent-skills" "install" "plurigrid/asi" "--agent" "claude")

  ;; Step 2: Load triads
  (doseq [triad startup-triads]
    (load-triad! triad))

  ;; Step 3: Verify total
  (let [total-sum (reduce + (mapcat #(map :trit (:skills %)) startup-triads))]
    (println (format "\nTotal GF(3) sum: %d ≡ %d (mod 3)" total-sum (mod total-sum 3)))
    (assert (zero? (mod total-sum 3)) "Total GF(3) violation!")))

(when (= *file* (System/getProperty "babashka.file"))
  (-main))
\stopverb}
}

\subtree{
\title{Related Skills}
\ul{
\li{\code{\startverb ruler-maximal\stopverb} (0): Post-bootstrap orchestration}
\li{\code{\startverb plurigrid-asi-integrated\stopverb} (0): Unified skill lattice}
\li{\code{\startverb skill-creator\stopverb} (0): Creating new skills}
}
}

\subtree{
\title{Scientific Skill Interleaving}
\p{This skill connects to the K-Dense-AI/claude-scientific-skills ecosystem:}

\subtree{
\title{Graph Theory}
\ul{
\li{\strong{networkx} [○] via bicomodule}
\li{Universal graph hub}
}
}

\subtree{
\title{Bibliography References}
\ul{
\li{\code{\startverb general\stopverb}: 734 citations in bib.duckdb}
}
}
}

\subtree{
\title{SDF Interleaving}
\p{This skill connects to \strong{Software Design for Flexibility} (Hanson & Sussman, 2021):}

\subtree{
\title{Primary Chapter: 10. Adventure Game Example}
\p{\strong{Concepts}: autonomous agent, game, synthesis}
}

\subtree{
\title{GF(3) Balanced Triad}
\pre{\startverb
load-skills (−) + SDF.Ch10 (+) + [balancer] (○) = 0
\stopverb}

\p{\strong{Skill Trit}: -1 (MINUS - verification)}
}

\subtree{
\title{Secondary Chapters}
\ul{
\li{Ch7: Propagators}
}
}

\subtree{
\title{Connection Pattern}
\p{Adventure games synthesize techniques. This skill integrates multiple patterns.}
}
}

\subtree{
\title{Cat# Integration}
\p{This skill maps to \strong{Cat# = Comod(P)} as a bicomodule in the equipment structure:}

\pre{\startverb
Trit: 0 (ERGODIC)
Home: Prof
Poly Op: ⊗
Kan Role: Adj
Color: #26D826
\stopverb}

\subtree{
\title{GF(3) Naturality}
\p{The skill participates in triads satisfying:}

\pre{\startverb
(-1) + (0) + (+1) ≡ 0 (mod 3)
\stopverb}

\p{This ensures compositional coherence in the Cat# equipment structure.}
}
}
}